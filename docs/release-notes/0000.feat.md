Add {meth}`~anndata.abc.CSRDataset.aread_rows`, plus {attr}`~anndata.abc.CSRDataset.indptr` and {attr}`~anndata.abc.CSRDataset.indices_dtype`, so a caller already inside an event loop can read whole rows concurrently and into buffers it owns. Backed CSR row selections are now read once each and in order, and are handed to the store as contiguous ranges rather than per-element coordinates whenever the rows are dense enough to make ranges the cheaper description -- which is also what lets the `zarrs` pipeline take its Rust path {user}`selmanozleyen`
