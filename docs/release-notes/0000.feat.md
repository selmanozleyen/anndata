Add {meth}`~anndata.abc.CSRDataset.aread_rows`, plus {attr}`~anndata.abc.CSRDataset.indptr` and {attr}`~anndata.abc.CSRDataset.indices_dtype`, so a caller already inside an event loop can read whole rows concurrently and into buffers it owns. Backed CSR row selections are now read once each and in order, which makes a scattered or repeated selection describable to the store as contiguous ranges {user}`selmanozleyen`
